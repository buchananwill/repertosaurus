---
title: "Session 11: The Habit Arc Gets a Roadmap"
type: commentary
area: journal
status: active
date: 2026-09-23
---

# Session 11: The Habit Arc Gets a Roadmap

**This is the head of the thread**, continuing [session 10](./2026-09-23-session-10.md).
Decisions continue at **D19** and findings at **F1**, both in the habit arc's series that session
10 opened.

## Reading order for a fresh context

1. §4 Next work below.
2. [habit-arc-roadmap.md](../roadmap/habit-arc-roadmap.md): §3 (the frontier rule), §4 (the
   gates) and §6 (the status board). **Do not read §5 end to end.** Read only the rows for the
   packages you are about to dispatch.
3. The vision [from-logger-to-nudger.md](../vision/from-logger-to-nudger.md), in full, before
   writing any spec.
4. **Do not read session 10 or earlier journals end to end.** Session 10's D12–D17 are cited by
   the roadmap where a spec needs them.

## 1. The arc's charter

Unchanged from [session 10 §1](./2026-09-23-session-10.md). The app **supports a long-term
routine; it assists and never imposes**, and the one-tap log is sacred.

The user's instruction this session (2026-09-23): the arc lead is to "create a roadmap to act as a
best-guess critical path through these features, allowing us to keep an active frontier without
excessive effort from me to re-inject momentum to the arc."

## 2. State as at bootstrap

- Branch `feature/repertoire-editing` at `1a28fdb`, and the tree is clean. **The branch is two
  commits ahead of `origin`** (`c716923`, `1a28fdb`): the local branch is not pushed. `main` and
  `origin/main` are still at `86c70c2`.
- **Measured this session:** `:shared:testDebugUnitTest --rerun-tasks` gave BUILD SUCCESSFUL,
  **386 test cases across 29 suites, 0 failures or errors**. The result XML is timestamped
  2026-09-23 21:03 (F21 check done).
- **Instrumented tests were not re-run.** The figure of 75 is inherited from session 09 and
  unmeasured this session.

## 3. Operational manual

Carried by reference from [session 09 §3](./2026-09-23-session-09.md): the emulator
`Pixel_3a_API_33_x86_64` headless, a shared machine, `--max-workers=2`, and slowness is contention,
not failure. F21 and F31 from session 10 §3 still apply.

Added this session:
- The shared-core baseline is `./gradlew :shared:testDebugUnitTest --max-workers=2 --rerun-tasks`
  from Git Bash.
- Count the results with a grep for `<testcase ` under
  `shared/build/test-results/testDebugUnitTest`, and for `<failure|<error `.

## 4. Next work

**As at the close of session 11. The next session starts here.**

**The roadmap is the plan.** Apply its frontier rule (§3). As at this entry, the frontier is:

1. **P1, the schema-3 spec**, which is **waiting on hard gate G1** (the four triage sorts). Ask it
   first.
2. **P3, the rating vocabulary and colour-ramp primitive.** Write its short spec, then dispatch
   it. It unblocks P5 and P6.
3. **P4, the Suggest spec.** It proceeds on the G4 and G5 defaults unless the user overrides them.
4. **P13a, beauty exploration.** Start the ramp mockups as artifacts.

**The gates are asked once, batched, in the conversation.** The user's answers land here as
decisions.

## 5. Open items

- G1–G11 in the roadmap's §4. Only **G1** and **G10** are hard.
- Whether to push the two local commits, and whether to fast-forward `main` (G11). These are the
  user's call.

## Decisions

- **D19. The habit arc is run from a roadmap**:
  [habit-arc-roadmap.md](../roadmap/habit-arc-roadmap.md), in a new `Notes/roadmap/` domain
  (`type: workflow`, `area: product`), added to the root index. **It is owned by the lead and
  amended in place.** The vision outranks it. It is not Piste Perfect's roadmap schema (session 10,
  D9 still stands). It is a plain workflow document inside this project's existing schema.

- **D20. Schema 3 is the critical-path keystone, and it is one migration.** This confirms session
  10 §4's "bundle every data change" proposal. The four data changes are:
  - ratings;
  - feel 0–3 plus `duration_seconds` (one `practice_event` rebuild);
  - `suggestion_skip`;
  - the widened `saved_view.sort_order` CHECK (F1).

  **Reason:** each one touches the same append-only and merge rules (data-model 7–14) and the same
  S1/S13 obligations. Two of them need a table rebuild. Designing them once costs less than
  designing them four times, and a single v2 → v3 upgrade is the only one the user's phone has to
  survive.

- **D21. Work that needs no schema change ships ahead of schema 3, as an early lane.** This
  replaces session 10's strictly sequential proposal (Suggest → schema 3 → triage → timer →
  scorecards → beauty). The early lane is:
  - Suggest v1 (shuffle plus coldness and hotness; skips always discarded);
  - Scorecards v1 (counts);
  - the ramp primitive.

  **Reason:** the user asked for a frontier that stays active without their re-injection. A lane
  that has nothing to wait on is the cheapest way to keep one. It also puts the radar on the phone
  early, and the radar is the control whose feel most needs the user's hands.

- **D22. Gates are hard or soft.**
  - **A soft gate is asked once, with a stated default.** If it is unanswered when its spec is
    written, the spec adopts the default and records it as an assumption. An overturned default
    becomes a follow-up package, and it never halts a lane.
  - **A hard gate blocks only its own package.**

  Hard gates: **G1** (it fixes a CHECK constraint, so a late change costs a table rebuild) and
  **G10** (beauty is the user's to direct). This is CLAUDE.md's "state the assumption explicitly
  in the journal", applied as a standing procedure.

- **D23. The session screen is a serialised hot file** (roadmap §2 rule 4). Suggest v1, triage
  plus Suggest v2, and the timer each own it in turn. **Triage and Suggest v2 are bundled as P9**
  because both read ratings on that screen.

- **D24. Suggester tuning is a per-device preference by default (G6),** following views.md V19's
  reasoning for the home View. This keeps it out of schema 3. Per-View tuning is deferred, not
  rejected.

- **D25. G1 is answered, so the four triage sorts are fixed (user, 2026-09-23: "Yes, fine").**
  There are two leading keys, **priority-led** and **confidence-led**. Each has a **most in need
  first** direction and its **reverse**, which mirrors coldest/hottest.
  - This settles only the four **names** that schema 3's `saved_view.sort_order` CHECK must admit.
  - It does **not** settle the blend (how days-since enters). That remains P7's decision.
  - The exact enum spellings are fixed in the schema-3 spec.
  - **P1 has no hard gate left**, so it moves to the frontier.

- **D26. The G10 process is accepted (user, 2026-09-23: "acceptable").** The lead produces
  colour-ramp mockups first, as artifacts. **Nothing in P13 is built until the user has picked a
  direction.** G10 stays hard, but its procedure is agreed.

- **D27. The user authorised committing the tree and pushing (2026-09-23),** then beginning work.
  This covers the notes as they stand, and the two local commits `c716923` and `1a28fdb`. `main`
  is still not moved (G11).

## Findings

- **F1. Adding the triage sorts is a schema change.** Adding them means widening
  `saved_view.sort_order`'s `CHECK (sort_order IN ('COLDEST_FIRST', 'HOTTEST_FIRST'))`
  ([saved_view.sq](../../shared/src/commonMain/sqldelight/dev/repertosaurus/db/saved_view.sq)
  line 57). SQLite cannot alter a CHECK, so it is a table rebuild. views.md V17a already warns
  that a new `SessionOrder` constant has to change three things: the constant, the CHECK and the
  parse. **So the triage sort names must be fixed before schema 3 freezes.** That is why G1 is a
  hard gate on P1.

- **F2. The Python migration emits no `feel`,** so widening feel to 0–3 does not engage CLAUDE.md's
  two-implementations rule. The workbook's `feel` column is the **groove** (`extract.py:1502`
  writes `facts["groove"]`). The only mention of `practice_event.feel` in `tools/import/` is a
  verification check (`build.py:993`, `feel NOT BETWEEN 1 AND 3`). That check stays correct for
  emitted data, which is always null.

- **F3. Session 10 §4 says its own notes are uncommitted. They are committed** in `c716923`, and
  `1a28fdb` follows it, but neither commit is pushed. Session 10's entry is left as written (the
  journal is append-only). This entry's §2 is the correct state.

## In flight

- ~~**P3 implementation**~~ **Landed in the tree, uncommitted, under review.**
  - Implemented against [rating-scale.md](../decisions/rating-scale.md) RS1–RS16, with the new
    default `DANGER_TO_SAFE`.
  - **Measured by the lead: 398 shared tests, 0 failures** (`--rerun-tasks`, XML at 22:11).
  - The delegate reports **85 instrumented tests, 0 failures** (21:10). The lead has not
    re-measured this, because the emulator was handed straight to P2.
  - The mutation was convicted.
  - Screenshots are in `.scratch/p3/`.
  - **Reviewers running:** `kotlin-safety-correctness-reviewer`,
    `kotlin-style-decomposition-reviewer` and `compose-quality-reviewer`.
- **P2 (schema 3)**, dispatched against [schema-3.md](../decisions/schema-3.md) on the same tree.
  It owns the `.sq` files, `2.sqm`, `Uuid.kt`, `RepertosaurusRepository`, `SchemaCompatibility`
  and `SongMerge`, and it has the emulator.

## P3 review rulings

**F8. The safety review of P3 approves it, with 0 BLOCKING and 7 NOTEs.** The reviewer ran no
tests. Rulings:
- **Accepted into the P3 fix round** (dispatched once the style and Compose reviews are in):
  - **N1:** assert the snackbar text "· feel: certainly" in `FeelSheetTest`.
  - **N2:** replace `aStepIsIndexedByTheLevelsValue`, which restates the implementation, with
    literal values: `COLD_TO_HOT.step(NOT_AT_ALL) == 0xFF93BDE8` and
    `step(EXCEPTIONALLY) == 0xFFF09A7E`.
  - **N3:** pin `Heat.level(-1) == EXCEPTIONALLY` (D35's ruling). Clock skew can produce it.
- **No action:**
  - **N4:** the default is pinned elsewhere, and step 0 is shared by the approved values.
  - **N6:** test-only, and the same shape as every existing field.
- **Carried forward as briefing text, not fixes:**
  - **N5** (preference writes on the multi-threaded `io` can reorder): it goes in P14's brief,
    because onboarding calls the setter too. Serialise both setters there, if at all.
  - **N7:**
    1. Every `RatingSegmentedControl` in the ratings editor (P8) passes a **unique `tagPrefix`**.
    2. **Onboarding (P14) is composed inside the `LocalColourRamp` provider**
       (`RepertosaurusApp.kt` around line 220), or its picker silently shows the default.
    3. **When feel gains 0**, the repository's `require` widens **before or with** the UI. The
       UI must never widen first, or a "not at all" tap throws on the IO write path. P2 widens
       the repository (M9). The feel sheet's `FEEL_LEVELS` (`SessionScreen.kt:88`, `:732`) and
       `theFeelSheetOffersOnlyOneToThree` are then widened in a **small follow-up after P2
       lands**. The deferral in rating-scale §4 said "with schema 3". It is now "straight after
       schema 3", because P2's brief keeps it out of the UI.

**F9. The style review of P3 requests changes: 13 BLOCKING, 5 NOTE.** Its central claim is correct
and predictive:
- one enum preference cost about 12 edits across 6 files;
- `setColourRamp` is `setNoteSpelling` token for token;
- three more device preferences are queued: suggester tuning, scorecard scope and owner performer.

The rulings follow. They are the lead's, and each item is weighed against the packages queued
behind it.

- **Accepted, structural** (these change the shape later packages build on, so they are done now,
  before P5, P6 and P14):
  - **B1: split device preferences out of `SessionPreferences`.**
    - Add a `DevicePreferences` interface for app-wide display preferences (spelling and ramp
      now, the queued ones later). `AndroidSessionPreferences` may implement both interfaces over
      one file.
    - Move the flows out of `SessionViewModel` into a small holder that `RepertosaurusApp` takes
      directly.
    - One private generic replaces the copied flow and setter pairs.
    - **This also takes P3 N5** (write reordering): the generic serialises its writes, so
      onboarding inherits the fix.
  - **B2:** delete the duplicate androidTest fake and use `InMemorySessionPreferences`
    (and its `DevicePreferences` counterpart).
  - **B3:** add one core `enumByName` helper. `fromStored` and `parse` keep their names as
    one-line delegations. **Unifying the boundary form (`lastOrder(): String?`) is deferred**,
    because it reaches into the Views code for no queued benefit.
  - **B4:** move `FeelSheet`, `FEEL_LEVELS` and `SessionTags.FEEL` mechanically into
    `FeelSheet.kt`. This takes the feel sheet out of the hot file before P5 and the feel-0
    follow-up.
  - **B5:** add `Messages.logged(title, feel)` in the shared core, with a test. That test also
    covers P3 N1.
  - **B6 and N5:** one tappable-surface form: `RampRow` as `Surface(onClick, shape, border)`, a
    private `RatingSegment`, and the shape named once.
  - **B7:** extract `AppDrawerContent` and a `DrawerItem` wrapper. P6 and P14 both add drawer
    items.
  - **B13:** `ColourRamp.label` moves into the core, beside `RatingLevel.label`.
    **rating-scale.md RS4 is amended.**
  - **N1:** drop `RatingLevel.stored` and use `value`. **RS2 is amended.** Two names for one field
    is the defect.
  - **N2:** add a `heatColour(daysSince)` composable helper in `RatingUi.kt`, the one heat
    derivation that P5, P6 and P9 reuse.
  - **N4:** add an `EditingFixtures.app(...)` whole-app test fixture, because B1 changes the
    ViewModels every such test builds.
  - **B12:** split `RatingTest` into `RatingLevelTest`, `ColourRampTest` and `HeatTest`.
- **Accepted, comments** (B8–B11): fix the counted-list comments that are already wrong (B8),
  keep one copy each of RS8 and RS10 (B9), and cut restating KDoc down to decision pointers
  (B10, B11). **The project's density of decision citations stays**: a pointer such as
  "rating-scale RS12" is kept, and paraphrase of the spec goes.
- **N3** is the same as the safety review's N2, already accepted.
- **Sequencing against the in-flight P2:** the fix round runs in the same tree as P2.
  - **N1 (removing `stored`) is deferred to the feel-0 follow-up after P2 lands**, because P2
    may call `.stored`, and removing it now would edit P2's files mid-flight.
  - The fix round must not touch `RatingKind` or any file P2 owns.
  - `Rating.kt` is re-read immediately before it is edited.

**F10. The Compose review of P3 requests changes: 1 BLOCKING, 2 NOTE.**
- **B1, accepted, and settled differently from either proposal.** `levels: List<RatingLevel>`
  makes `RatingSegmentedControl` non-skippable. `List` is unstable, and strong skipping is off
  at Compose compiler 1.5.10. That matters because P8 runs the control two per row in a lazy
  list. The reviewer offered `ImmutableList`, which is a new dependency, or turning on strong
  skipping, which is an app-wide compiler change on a shared build. **The ruling is
  `lowest: RatingLevel = NOT_AT_ALL`, an enum and therefore stable.** It states RS10's only need
  (exclude 0) exactly. RS9 and RS10 are amended.
- **N1, accepted:** `staticCompositionLocalOf`. The ramp changes only on an explicit pick, a
  whole-tree recompose is RS8's wanted behaviour, and the local is read at many leaves. RS8 is
  amended.
- **N2** is the same as the style review's B13, already accepted.

**D38. The P3 fix round is dispatched to the P3 author**, which keeps its context, with the rulings
of F8, F9 and F10. It stays out of P2's files.

- **D39. Existing installs see onboarding once (user, 2026-09-23: "Yes").** P14 shows its
  sequence once on any install that has not completed it, including the user's phone.

- **D40. The ratings editor needs A–Z pagination or other search and filter tools (user,
  2026-09-23),** after viewing the canvas's ratings board: "A massive scroll of 400 songs is
  awful, but the size feels right, so pagination is probably the missing tool."
  - The row size and density are approved.
  - **P7's spec must include:**
    - A–Z pagination, one letter a page, with a letter strip;
    - search;
    - a filter for unrated or rated parts.
  - The Repertoire toggle list that the editor lives on has the same scale, so the same tools
    apply to it.

- **D41. The overall look is rejected as generic (user, 2026-09-23): "The general choice of fonts
  and styling says 'Claude made this'… can you take a punt on making it look NOT like you made
  it?!?"**
  - Round 1's look is **struck**: warm ivory, deep teal, Bricolage Grotesque with Atkinson
    Hyperlegible, soft rounded cards, and small uppercase tracked labels.
  - The ramps and their default stand (D36).
  - **P13a round 2 takes a deliberately different direction.** The lead's standing note for it:
    avoid the tells, which are warm-neutral ground, one muted accent, rounded bordered cards,
    tracked small-caps labels and a trendy grotesque paired with a humanist sans. Draw on the
    working musician's own visual world instead of product-design defaults.

- **D41, sharpened by the user the same day:** "a better way of expressing that is: it looks like
  it's following the Anthropic Brand Style Bible. That's bad."
  - **Standing rule for every visual in this project:** no Anthropic brand vocabulary. That means
    no ivory or cream ground near `#FAF9F5`, no clay or terracotta accent near `#D97757`, no
    slate-on-ivory, and no serif-display-over-sans pairing borrowed from it.
  - **The design canvas's own type instructions steer towards exactly that palette** when no
    design system is given, which is how round 1 drifted there. Any delegate or future session
    making mockups must be told this explicitly.
  - naming.md N11's "warm neutrals" and "terracotta" read the same way, so **N11's palette is
    advisory only for the app UI.** The logo brief is unaffected.

- **P13a round 2 is published on the same canvas**, in a new row under round 1. It is a
  **gig-flyer / risograph** direction:
  - a cool newsprint grey ground (`#E8E6E0`);
  - flat riso inks: blue `#2B4C9B`, fluorescent pink `#FF48B0` and a yellow `#FFE14D` for the one
    primary action;
  - near-black `#121212` hard 2–3 px rules;
  - **square corners throughout**, offset shadows and misregistration text-shadows;
  - Big Shoulders Display (heavy condensed poster type) for display and Barlow Semi Condensed for
    body;
  - heat badges as slightly rotated "stamps".

  Its boards are the session screen, the **ratings editor with A–Z pages, search and an
  All / Unrated / Rated filter** (interactive, per D40) and onboarding's ramp question
  (interactive, per P14). The ramps are unchanged (D36). Awaiting the user.

- **D42. The P3 fix round landed (F8–F10 applied).**
  - The delegate measured **419 shared tests, 0 failures**, and the instrumented tests compile.
    **The instrumented run is outstanding** until the emulator is free of P2.
  - The 419 includes P2's in-progress `SchemaThreeTest` (12). So the P3-only figure is not
    separately known, and the next lead measurement is on the merged tree.
  - **All ten interpretations are accepted:**
    - `DevicePreferences` lives in `session`;
    - `DeviceSettings` is a ViewModel;
    - `SongsScreen` takes the settings;
    - N5 is handled by a Mutex with latest-value persistence (`limitedParallelism` is
      experimental at coroutines 1.7.1);
    - the `FeelSheetTags` rename;
    - `feel?.value`, with `stored` kept until P2;
    - a transparent `RampRow`;
    - `DrawerItem` places its tag outside the inset;
    - the drawer comments are trimmed;
    - the fixture does not wait on boot.
  - The files it touched outside the original list were required by B1 and N4.
  - **P3 is not yet committable:** the instrumented run and screenshots of the new drawer and
    picker are owed.

## P2 hand-back

- **P2 landed M1–M20 in the tree, uncommitted.** The delegate measured **419 shared tests, 0
  failures** (21:29). **The upgrade on the device is proven:**
  - `user_version` went 2 → 3;
  - events 11 → 11 and voids 1 → 1, with every `(id, created_at, device_id)` identical;
  - `foreign_key_check` is empty and `integrity_check` is ok;
  - the app opens on the session screen.

  **The M18 mutation is convicted:** removing the void-holding steps fails both upgrade tests with
  `SQLITE_CONSTRAINT_FOREIGNKEY`.

- **F11. schema-3.md's "no code change to `tools/import/`" was wrong (the lead's error),** and
  the delegate demonstrated it:
  1. `assert_roster_covers_schema` is FATAL on `part_rating` and `suggestion_skip`, because
     neither is in `NOT_POPULATED`.
  2. The build inserts into `practice_event` **by position with 9 values** (`build.py:790-792`),
     which fails against the 10-column table.

  **I reasoned "reads the schema from the `.sq` files, so it picks schema 3 up by construction"
  without opening the insert.** The fix belongs to P2's owner:
  - add both tables to `NOT_POPULATED`;
  - use a column-named insert;
  - widen the `build.py:993` verification check to `feel NOT BETWEEN 0 AND 3`, to agree with the
    schema, even though emitted feel is always null.

  No id derivation is touched, so the two-implementations rule is not engaged.

- **F12. The Python build's end-to-end run is blocked by permission, and correctly so.** It reads
  the user's review workbook in their Dropbox as an input. The permission classifier refused the
  delegate's read as personal data. **This is not to be routed around.** It goes to the user:
  either they allow the read, or they run the build themselves.

- **F12, clarified to the user (2026-09-23).** The input is `D:\Dropbox\Work\Gigs\review.xlsx`,
  the user's hand-edited review sheet from the original workbook import. It is **not** the phone's
  practice database.
  - **A full build also writes `.scratch/repertosaurus.db`** (`OUT_DB`), which every delegate is
    told never to overwrite. An end-to-end run would need its output redirected first.
  - **The lead's recommendation: skip the end-to-end build until a re-import is actually
    wanted.** The app's own v2 → v3 upgrade, the path the phone takes, is proven.

- **F13. 9 of 85 instrumented tests fail, all stale fixtures from the version bump** (S1 and S13
  working as designed):
  - five `DatabaseHolderBootTest` assertions hard-code version 2;
  - four tests use `DatabaseFixtures.writeVersionOne`, which downgrades **from the current
    schema**, so "v3 minus the v2 changes" throws `table part_rating already exists` on upgrade.
    The shared `SchemaV1Fixture` was already fixed the same way by P2.

  **The app reported `Unloadable` rather than crashing** (S8 held). Assigned to P2's owner: bump
  the literals, and build the v1 fixture from the v2 shape.

- **D43. Rulings on P2's interpretations: all nine are accepted.**
  - **1:** M20 against the merge's `carry` rule. A tombstoned survivor rating is revived by the
    loser's level, and the loser's ratings are cleared as its children are (R38a).
  - **2:** `logPractice` is the real name.
  - **3:** column position after `note`.
  - **4:** no `IN` lists, because of the 999-variable cap.
  - **5:** clearing nothing returns false.
  - **6:** the sync queries were added for parity.
  - **7:** no `FEATURES` entry. A v2 file is `Upgradable`, and the wording only matters on a
    refusal.
  - **8:** test placement.
  - **9:** `.value`.

- **D44. Order of the remaining P2/P3 work**, serialised on the one emulator:
  1. **P2 fix round:** F11 and F13, then the **full instrumented suite on the merged P2+P3 tree**.
  2. **The feel-0 follow-up, by P3's owner:**
     - `FEEL_LOWEST` becomes `NOT_AT_ALL`;
     - remove `RatingLevel.stored`;
     - update `FeelSheetTest` and the stale `FeelSheet.kt:42` comment;
     - take screenshots of the drawer, the picker and the feel sheet with "not at all".
  3. The lead re-measures, and P2 and P3 are committed together, **subject to the user's
     approval to commit.**

  The P2 reviews run now, in parallel, because they are read-only.

- **F14. The P2 safety review requests changes: 1 BLOCKING, 5 NOTE, and no code defects.** It
  confirmed several properties by reading, not by running:
  - M18's order is safe on both legacy (3.19) and modern `RENAME` semantics;
  - bytes are preserved;
  - the indexes match the v2 dump;
  - M7 revives by key;
  - M14's tests can fail.

  Rulings:
  - **B1, accepted:** the stale `sqldelight/README.md` (it still says version 2, lists neither
    new table, and lacks the M18/M2 lessons) goes to P2's fix round.
  - **N3, N4 and N5, accepted into the same round:**
    - a test where the loser's rating is a tombstone;
    - the 1 → 3 chain run with a void, foreign keys on, inside a transaction;
    - a literal `partRating` id pinned.
  - **N1, recorded for P9:** the bulk skip count scans `suggestion_skip`, because M12's index
    leads with `song_id`. If Suggest v2 measures it as slow, an `(instrument_id, …)` index goes in
    a later `.sqm`.
  - **N2, recorded for phase 2 sync:** `part_rating.applyMerged` is `INSERT OR REPLACE` on a table
    whose unique key is not its id. Sync must compare **by key** under data-model 11 before
    applying. `song_performer` has the same property.

- **F15. The P2 style review requests changes: 10 BLOCKING, 3 NOTE.** Its central claim is
  correct:
  - `RepertosaurusRepository.kt` grew from 630 to 830 lines, past editing.md E30's 698-line
    trigger;
  - `SongMerge` now reaches back into the repository's file for `Part` and `writeRating`;
  - the rating and skip surface has **zero production callers**, so it will never be cheaper to
    move.

  **Rulings, all to a second P2 fix round**, dispatched after the current round reports so the
  two are not tangled:
  - **B1:** move ratings into `data/PartRatingStore.kt` and skips into
    `data/SuggestionSkips.kt`, exposed as `repository.ratings` and `repository.skips`.
    `SongMerge` takes the store in its constructor. The rating and skip tests move out of
    `SchemaThreeTest`.
  - **B2:** one SQL statement for M14. The single-part read delegates to the bulk read. The `''`
    sentinel goes.
  - **B3:** `enumByNameOrNull` in `Enums.kt`, used by `RatingKind.fromStored`.
  - **B4: one decoder for a `part_rating` row, with one policy.** **An undecodable row is skipped,
    never thrown**, in both the read and the merge carry, per schema-compatibility S8. No
    database state may crash the app, and a merge is a user action on that state.
  - **B5:** one private `derivedKey(table, vararg keys)`. The two public `junction` overloads
    delegate to it, and the four-key overload is deleted.
  - **B6:** a `requireInserted()` helper in `Writes.kt`, used by both callers.
  - **B7:** comments cut to M-number pointers plus the irreducible why. The process narration in
    `2.sqm` and `SchemaV1Fixture` is deleted. **2.sqm's comment is edited before it ships**,
    because it is not frozen until then.
  - **B8:** shared test helpers `SqlDumps.kt` and `DriverReads.kt`.
  - **B9:** delete the unused `PartRatings.get`.
  - **B10: `Part` is the single-part parameter everywhere. The bulk convention is overloads on
    `Collection<Part>`**, for both ratings and skips.
  - **N2:** trim the M2 comment to its load-bearing sentence.
- **N1 goes to the feel-0 follow-up (D44 step 2):** `feel: RatingLevel?` runs through
  `SessionCoordinator` and `logPractice`, and the `0L..3L` literal goes. It crosses into P3's
  layer, and P3's owner is doing feel-0 anyway.
- **N3, deferred:** renaming `JUNCTION_KEYS` gets its own later pass, as the reviewer advised.

- **P2 fix round 1 landed:** F11 (`build.py`), F13 (the androidTest fixtures) and the F14 items.
  - **Lead-verified: instrumented 85/85, 0 failures** (XML 21:35:49), on the merged P2+P3 tree
    **including P3's fix round.** That closes P3's outstanding instrumented run.
  - The delegate reports **423 shared tests, 0 failures** (21:37). The lead will re-measure after
    round 2.
  - **The literal `part_rating` id `eb97eab6-2ac2-5d11-b8e5-55b429631a91` is cross-checked by an
    independent Python `uuid5`,** whose `vocal` id reproduces the known seed.
  - `build.py` passes `assert_roster_covers_schema` and the named insert. **The full Python build
    is still unrun** (F12, waiting on the user).
  - An editor diagnostic reporting `INSERT_COLUMNS` as undefined was checked: it is defined at
    module level (`build.py:514`), so the diagnostic was stale, seen mid-edit.
- **D45. The triage spec is [triage.md](../decisions/triage.md) (T1–T10).** It adopts the G9
  default (feel is not used) and the G12 default (the owner performer as a device preference).
  Its lead-authored judgements:
  - **one editor with two entry points.** The View entry exists because capability data is
    vocal-only, so without it a guitar View could never be triaged (T1);
  - letters file after dropping a leading "The", "A" or "An" (T3);
  - the paging, search and filter also go on the toggle list (T5a);
  - **the sort becomes mode plus direction** (T6);
  - **lexicographic comparators with staleness as the tie-breaker**, which is readable aloud, and
    the four-step keys make neglect do real work (T7);
  - **unrated sorts last in both directions, never as 0** (T8);
  - **it amends views.md V12a deliberately**, closing F4 (T9).

- **P2 fix round 2 (F15) landed.**
  - **Lead-verified: instrumented 85/85, 0 failures** (XML 21:47:06).
  - The delegate reports **424 shared tests, 0 failures** (21:44), and `IdsTest` is 22/22 with
    the literal pinned.
  - `RepertosaurusRepository.kt` is down to about 655 lines. Ratings and skips are in
    `PartRatingStore` and `SuggestionSkips`.
  - The B4 skip policy is proven with a genuinely undecodable row, inserted under
    `ignore_check_constraints`.
  - **All five interpretations are accepted**, including #3: a live but undecodable *survivor*
    rating still counts as held, so the merge preserves the survivor's row rather than
    overwriting it. That is the conservative direction.
- **The feel-0 follow-up (D44 step 2) is dispatched to P3's owner.**
- **D48. The feel-0 follow-up landed, so P2 and P3 are complete and ready to commit.**
  - **Lead-measured: 425 shared tests, 0 failures** (`--rerun-tasks`, on the merged tree).
  - **Lead-verified: instrumented 85/85, 0 failures** (XML 21:52:28).
  - The lead viewed `.scratch/p3/feel-sheet-not-at-all.png`: four levels, 0–3, in the default
    Danger → safe ramp.
  - Feel is a `RatingLevel?` from the sheet to the SQL insert, and `RatingLevel.stored` is gone.
    **All four interpretations are accepted:**
    - `lowest` is now used only by its own test;
    - the feel=4 repository test is gone, because the type forbids it;
    - the test placement stands;
    - the read side keeps `Long`.
  - **F16, a carry-forward:**
    - **"exceptionally" only just fits** a quarter-width segment on the 393 dp Pixel 3a, and
      narrower phones or larger font scales may clip it. P8's ratings editor and P13's theme
      must handle it, with a two-line label or a smaller label size. Nothing tests it yet.
    - **RS9's `lowest` has no production caller.** P8's brief removes it if it is still unused
      then.
  - **Committing waits on the user's go-ahead**, asked for in the conversation.

- **P13a round 3 is published** in a new row on the canvas, following D47. The direction is
  called "handed on":
  - **everything rectilinear, with no rotation**;
  - the hard shadows kept, and extended to the badges and buttons;
  - **texture**: an SVG grain over the whole screen and a halftone dot pattern on the ink blocks;
  - a slight uneven gradient on the indigo header;
  - a palette of **indigo** `#25338A`, **madder rose** `#D2456F`, **yellow ochre** `#E3A92F`,
    charcoal `#1B1A17` and a stone ground `#E6E3DA`. None of it is Anthropic ivory or clay;
  - **motif: the stencilled hand**, the oldest image humans made, used as a literal "handing on".
    It appears as a small sprayed mark in the header and as a cluster on onboarding's welcome
    ("Every song was handed on", with *tradere*).

  The hands in the cluster sit at natural angles. They are decoration on a wall, not UI, but
  that is flagged to the user against the "no jaunty angles" ruling. Awaiting the user.

- **D49. The round 3 look is approved (user, 2026-09-23): "Lose the dot-matrix 'paper
  texture'. I think we can otherwise move on with this style. It has much more character than the
  version currently on my phone."**
  - **"Dot-matrix" is read as the halftone dot pattern** on ink blocks, badges and buttons, which
    is removed. The fine SVG grain and the header gradient stay. This is the lead's reading,
    stated to the user for correction.
  - **G10 is closed for the look.**
  - **Roadmap change: P13 splits.**
    - **P13b, "Theme foundation", is pulled forward to now.** It covers the palette, type, square
      shapes, hard shadows, grain, gradient and the hand mark as Compose tokens and primitives.
      Every UI package still to come (P5, P6, P8, P14) is then built in the approved style,
      instead of being built in Material defaults and restyled later.
    - **P13 becomes a closing polish pass.**
  - The visual contract will live in a new decision spec, `visual-identity.md`.

- **D50. The visual identity spec is [visual-identity.md](../decisions/visual-identity.md)
  (VI1–VI17).** Its lead-authored judgements:
  - pressed buttons sink onto their shadow instead of rippling (VI12);
  - the grain is one seeded tileable bitmap under `BlendMode.Multiply` (VI10);
  - the hand is sprayed procedurally from seeded dots and cached (VI11);
  - **the onboarding hands are the only rotated elements** (VI11), a carve-out stated to the
    user;
  - labels must survive a 1.3 font scale, which answers F16 (VI8);
  - the theme is light only until P13 (VI17);
  - the hand mark is the UI motif, not a replacement for the logo.

  **The fonts are acquired by the lead** (CLAUDE.md: acquire external data at the top level) into
  `.scratch/fonts/`:
  - Big Shoulders Display (variable) and Barlow Semi Condensed Medium, SemiBold and Bold, from
    `github.com/google/fonts`, `ofl/`;
  - both OFL licence texts.

  The file sizes match the repository listing. **Critical path amended:** P13b runs before P5.

- **D51. Standing authorisation (user, 2026-09-23, signing off for the night): "I'm giving you
  the all clear now to move forward with all the roadmap. Commit regularly. I'm signing off for
  the night, so you have approval to steer forward all the discussed work."**
  - **This covers:**
    - committing each package once it is green and reviewed;
    - pushing `feature/habit-arc`;
    - dispatching every package on the roadmap, with the soft-gate defaults adopted as recorded.
  - **It does not cover:**
    - moving `main` (G11 stays the user's);
    - the Dropbox read (F12);
    - anything outside the discussed roadmap.
  - Visual choices beyond the approved round 3 follow visual-identity.md and are reported in the
    morning.

- **D46. The onboarding spec is [onboarding.md](../decisions/onboarding.md) (OB1–OB7).** Its
  lead-authored judgements:
  - it shows only after the database is `Ready`, and never over `RecoveryScreen` (OB1);
  - the performer step is skipped automatically when there are no performers (OB2);
  - skipping marks onboarding done for good (OB3);
  - each answer is written as it is chosen, with "done" written last (OB4);
  - the drawer gains "Who you are" (OB6).
- **P2 fix round 2 (F15) is dispatched to P2's owner.** (Superseded by the entry above.) The feel-0 follow-up (D44 step 2) waits
  for it, because N1 touches `logPractice`.

- **D47. The user rules on round 2 (2026-09-23): "I commend the boldness in gear change."**
  - **Keep the bold gig-flyer direction and the hard drop shadows.**
  - **Remove the "jaunty angles": everything is rectilinear.** No rotated badges or elements.
  - **Add texture or gradient, something "hand-made" or "organic".**
  - **The brand's deeper theme, in the user's words:** "Music is maybe the oldest art we have.
    Birds sing. Humans probably made music before they fully had language or painting… Repertosaurus
    needs to evoke some of that ancient tradition… 'tradition' comes from 'handing on', so we want
    to make the user feel like they're part of a tradition, holding hands with musicians since
    humans began."

  **This is a vision-level statement, so it is written into the vision's Beauty section as the
  user's words.** Round 3 follows from it.

## Findings (continued, 2)

- **F7. The session list keeps its scroll position across a sort flip.** Reported by the P3
  delegate, and it predates P3. After "Hottest first", the rows moved to the top were above the
  viewport until the user scrolled up. **Worth fixing in P9**, which adds four more sorts to the
  same toggle: a sort change should scroll to the top.

## Findings (continued)

- **F4. Performer-scoped ratings collide with views.md V12a.** V12a says "the sort has no
  performer dimension, and must not grow one", because `practice_event` carries no performer.
  But the user ruled (session 10, D1) that priority and confidence belong to a
  song–performer–role. A triage sort, or a suggester weighting, over a View's rows must therefore
  pick **whose** rating to read.
  - When the View names a filter performer, the natural answer is that performer on the View's
    practice instrument.
  - **When it names none, there is no answer without an owner ("me") concept.** views.md §3
    deferred that concept because it had "no caller". It now has two callers, P4 and P7.

  V12a's rationale (events are the owner's) is untouched: the *staleness* key stays per
  instrument. The ratings are an additional input, which is why this is an amendment for P7 to
  make deliberately, not a defect. **Raised as roadmap gate G12. It does not affect schema 3**,
  whose tables key on the triple either way.

- **F5. The 2 → 3 migration runs with foreign keys enforced.**
  `DatabaseFactory.createDriver` enables them in `onConfigure`, before `onUpgrade`, and `PRAGMA
  foreign_keys` cannot change inside the upgrade transaction.
  `practice_event_void` references `practice_event`, so a naive rebuild of `practice_event`
  (`DROP TABLE` performs an implicit `DELETE`) **fails on any database holding a void**, which
  includes the user's. `1.sqm` never met this, because nothing references `song_performer`.
  Written into [schema-3.md](../decisions/schema-3.md) M18 as a property, with a known-good order
  as advice, and gated by a mutation test.

- **P13a round 1** is published as a private design canvas:
  [Repertosaurus colour ramps](https://claude.ai/artifact/4Hq2yaxYmEdqeczZwacf5e). It holds:
  - the three RS4 ramps compared, each with a sample control and heat badges;
  - a session screen with heat badges, with a ramp switch in Tweaks;
  - an interactive ratings editor.

  The candidate identity it shows, for the user to react to:
  - a warm ivory ground (`#F4EFE6`), ink navy text (`#1F2A3A`) and a deep teal accent
    (`#1F6F6B`), from naming.md N11;
  - Bricolage Grotesque for display and Atkinson Hyperlegible for body. The body face was chosen
    for legibility on a music stand.

  **Nothing here is a decision until the user rules** (D26).

## Decisions (continued)

- **D30. The schema-3 spec is [schema-3.md](../decisions/schema-3.md) (M1–M20).** Its
  lead-authored judgements, each argued in its decision:
  - priority and confidence are **separate rows by `kind`** in one `part_rating` table, so a
    priority edit and a confidence edit made offline never overwrite each other (M4);
  - the rating id is **derived from the triple plus kind**, and ratings key on the triple's ids,
    **never `song_performer.id`**, whose ids are forked by S3 (M5, M6);
  - unrated is a **tombstone**, and re-rating revives the row (M7);
  - `duration_seconds` is 1–86400, where null means an untimed tap (M10);
  - skips since last practised compare on **`created_at`**, and a voided log does not reset the
    count (M14);
  - the sort names are `TRIAGE_PRIORITY`, `TRIAGE_CONFIDENCE` and their `_REVERSED` forms. P2
    widens the CHECK, and P9 adds the enum constants (M15, M16);
  - song merge carries durations and ratings (A's rating wins), not skips (M20).

- **D32. The Suggest spec is [suggest.md](../decisions/suggest.md) (SG1–SG16).** It adopts
  the soft-gate defaults for G4 and G5 as assumptions the user may overturn: five spokes,
  coldness and hotness cancelling, all radii zero, skips off, count shown when on, and a 5 s undo.
  Its lead-authored judgements:
  - **`weight = exp(ln 16 · Σ r·f)`**, so all-zero is *exactly* uniform and the polygon's size
    and shape carry the meaning the vision gives them (SG8);
  - a **no-repeat deck** per open sheet (SG6);
  - the pool ignores the search query (SG5);
  - **dismissing is never a skip** (SG3);
  - **unrated contributes nothing** (SG9);
  - the radar is **in the sheet** (SG11).

  **It amends roadmap P5:** locked spokes are drawn and greyed, not absent.

- **D33. The Scorecards spec is [scorecards.md](../decisions/scorecards.md) (SC1–SC15).** It
  adopts G7's default (a day with at least one live event). Its lead-authored judgements:
  - **the rating ramp is not used on the grid**, because an amount is not a rating, and a
    one-log day painted "not at all" red would be the accusation the vision forbids (SC6);
  - **no streaks** (SC12);
  - the weakest weekday is shown, never called out (SC9);
  - reliability is clipped to the first event (SC8);
  - events on soft-deleted songs count, which deliberately differs from E39 (SC3).

- **D34. The single emulator is the serialisation point for implementation packages.** Two
  delegates running instrumented tests on one device would clobber each other. When P3 lands, the
  dispatch order is:
  1. **P2 alone**, because it is the critical path;
  2. **P5 and P6 together**, each pinned by `adb -s` to its own AVD: `Pixel_3a_API_33_x86_64`
     and `Pixel_4_API_30`.

  The shared machine may make two emulators too slow. If so, run them one after the other.

- **F6. P3's file list was wrong (the lead's error).** The `NoteSpelling` precedent that
  rating-scale.md RS5, RS7 and RS16 cite runs through `SessionViewModel`. It holds a
  `MutableStateFlow(preferences.noteSpelling())` and a `setNoteSpelling()` that
  `RepertosaurusApp` collects, and `RepertosaurusApp` cannot reach `SessionPreferences` any other
  way. I cited the precedent without opening the file that makes it work. **The delegate stopped
  instead of routing around it.** The route it declined, reading `AppGraph.of(context).preferences`
  from a composable, would have made two instrumented tests write the real app's preferences file.
  This is the skill's "verify a cited precedent before repeating it", learned again.

- **D35. Rulings on P3's hand-back:**
  - **`SessionViewModel.kt` is added to P3's owned files**, for the ramp flow and setter only,
    copied from the note-spelling shape as the delegate proposed. The stale E2 comment at the
    `FeelSheet` call site (`SessionScreen.kt` around line 300) is also P3's to fix.
  - **All ten of its interpretations are accepted:**
    - `tagPrefix` as a trailing parameter;
    - the button stays "Log it";
    - the ramp display names are the delegate's wording;
    - the picker is a bottom sheet;
    - the segment styling is as built;
    - a negative `daysSince` counts as EXCEPTIONALLY;
    - the `ColourRamp.step` helper is kept;
    - the KDoc rewording stands.
  - Measured by the delegate at hand-back: **398 shared tests and 84 instrumented, 0 failures**
    (up from 386 and 75). The heat-boundary mutation was convicted. The lead re-measures after
    the wiring pass.

- **D36. The ramps are approved and the default changes (user, 2026-09-23),** after they viewed
  the P13a canvas: "all three are good… My personal preference would be Danger → Safe, so perhaps
  make that the default."
  - **`ColourRamp.DEFAULT` becomes `DANGER_TO_SAFE`**, which **amends rating-scale.md RS5**
    (struck in place). The vision's colour section carries a dated note from the user.
  - The in-flight P3 delegate was sent the change mid-package.
  - **The provisional hex values in RS4 stand as approved.**
  - **The overall look is not ruled on:** the ivory ground, the teal accent and the Bricolage /
    Atkinson type. G10 stays hard for that part.

- **D37. Choosing a ramp is a first-run onboarding question (user, 2026-09-23): "make that
  choice one of the 'onboarding' questions for a new user."** The app has no onboarding today.
  **This opens roadmap package P14.** The lead adds the owner-performer question (G12) to it,
  because it is the other question that needs asking exactly once. The standing test applies:
  onboarding is skippable in one tap, never blocks logging, and every answer stays changeable
  from the drawer.

- **D31. P2 is sequenced after P3 lands, not run in parallel with it.** P2's repository surface
  reuses P3's `RatingLevel` (M19). Both packages need the single emulator for their
  verification. Roadmap P2's dependencies are amended.

- **D28. The arc's code goes on `feature/habit-arc`,** cut from the pushed `f6b4211`. The
  editing branch is closed at its pushed head.
- **D29. P3's spec is [rating-scale.md](../decisions/rating-scale.md).** Its lead-authored
  judgements are:
  - the ramps live in the shared core as ARGB `Long`s, so desktop and web reuse them;
  - the ramp is read through a `CompositionLocal` only;
  - heat thresholds are 0–3 / 4–13 / 14–29 / 30+ or never, which keeps today's 30-day boundary;
  - the feel sheet stays on 1–3 until schema 3.

  The hex values and thresholds are provisional and belong to P13a.
